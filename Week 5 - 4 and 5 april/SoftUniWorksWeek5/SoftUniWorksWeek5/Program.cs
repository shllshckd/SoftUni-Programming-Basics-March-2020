using System;

namespace SoftUniWorksWeek5
{
    class Program
    {
        static void Main(string[] args)
        {
            int count = int.Parse(Console.ReadLine());
            int leftNum = 0;
            int rightNum = 0;

            for (int i = 0; i < count; i++)
            {
                leftNum = int.Parse(Console.ReadLine());
            }
            

            for (int i = 0; i < count; i++)
            {
                rightNum = int.Parse(Console.ReadLine());
            }

            if (rightNum == leftNum)
            {

            }

        }
    }
}
